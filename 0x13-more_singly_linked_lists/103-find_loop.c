#include "lists.h"
#include <stdio.h>

/**
  * find_listint_loop - loop  function
  * @head: first node in the list
  * Return: success
  */
listint_t *find_listint_loop(listint_t *head)
{
	head++;
	return (0);
}
